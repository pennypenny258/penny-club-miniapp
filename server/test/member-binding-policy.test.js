'use strict';
const test=require('node:test'),assert=require('node:assert/strict');
const {WECHAT_IDENTITY_CAPABILITIES,createBindingReviewCandidate,assertOperatorConfirmedBinding}=require('../src/auth/member-binding-policy');

test('WeChat login capability contract never claims nickname, WeChat ID, phone or group state',()=>{for(const field of ['wechat_nickname','wechat_id','phone_number','group_nickname','group_membership'])assert.equal(WECHAT_IDENTITY_CAPABILITIES.loginDoesNotProvide.includes(field),true);assert.equal(WECHAT_IDENTITY_CAPABILITIES.phoneMode.includes('explicit_getPhoneNumber'),true);assert.equal(WECHAT_IDENTITY_CAPABILITIES.groupMembershipMode,'crm_operator_confirmed_only')});
test('binding candidates remain pending and reject client or WeChat-API identity shortcuts',()=>{const subjectHash='a'.repeat(64);assert.throws(()=>createBindingReviewCandidate({subjectHash,wechatIdSource:'wechat_login_api'}),/会员填写或运营导入/);assert.throws(()=>createBindingReviewCandidate({subjectHash,groupStatusSource:'wechat_group_api'}),/运营/);assert.throws(()=>createBindingReviewCandidate({subjectHash,phonePresent:true,phoneAuthorizationVerified:false}),/主动授权/);const result=createBindingReviewCandidate({subjectHash,phonePresent:true,phoneAuthorizationVerified:true,wechatIdSource:'member_self_reported',groupNicknameSource:'operator_import',groupStatusSource:'operator_confirmed'});assert.equal(result.status,'pending_operator_confirmation');assert.equal(result.automaticMemberBinding,false);assert.equal(result.automaticMembershipActivation,false);assert.equal(result.rawProfileReturned,false)});
test('only an operator-confirmed CRM binding can resolve an internal member',()=>{assert.throws(()=>assertOperatorConfirmedBinding({status:'pending_operator_confirmation'}),error=>error.code==='IDENTITY_NOT_BOUND');assert.deepEqual(assertOperatorConfirmedBinding({status:'operator_confirmed',memberId:'member-fixture',subjectHashVerified:true}),{memberId:'member-fixture',operatorConfirmed:true})});
