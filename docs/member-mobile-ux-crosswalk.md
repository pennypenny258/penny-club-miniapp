# 会员端移动体验需求对照

## 目标与范围

本轮只重构会员端移动体验，浏览器预览和原生微信小程序统一为四个固定底部入口：动态、活动、Agents、我的。运营后台继续采用既有六模块信息架构。本文件以既有 PRD、安全规则与本轮移动原型的并集作为验收依据。

## 页面与既有能力对照

| 新入口 | 此前 PRD / 已有能力 | 本轮实现 | 正式上线依赖 |
| --- | --- | --- | --- |
| 动态 | 活动、行业报告、群聊精华、线上回放均已有安全数据 | 默认首页；安全搜索；按时间聚合活动、资料和已人工审核公开机会；收藏；活动登记 | 微信登录态、订阅消息模板、生产搜索索引 |
| 活动 | 已有线上、线下、混合活动与报名/归档模型 | 共用顶部搜索；展示主题、形式、状态、时间、地点、分享者和参会者；会议链接按报名与时间门禁开放；已完成活动进入回放或纪要 | 正式会议服务、活动真实数据与归档文件 |
| Agents | 已有服务端人工审核与公开/私密投影 | 单一需求窗口；提交“我是谁 / 背景 / 具体需求”；前台只提供公开与私密匹配两种选择；语音入口不伪造转写 | 规则/AI 匹配、人工分发、真实 ASR、联系方式分级披露审批 |
| 我的 | 会籍综合判定、差异化续费、自助公开资料更新、在职核验、申请对接 | 演示身份占位；会籍和本人续费报价；内部资料；公开资料待审核更新；在职核验；对接决定；收藏；安全退出 | 微信联合登录、微信支付、生产 CRM 写入、私有文件、隐私授权留痕 |

## 知识库边界

- 研究报告、群聊精华、书籍和常用文件不进入第一版会员端活动页，继续沿用飞书。
- 线上活动完成后，可通过活动归档入口关联回放、纪要文档或录屏；前台不展示资料标签和“已发布”等后台状态。
- 既有资料导入、审核、存储与下载控制仍保留在运营后台，避免本轮前台收缩误删后续可复用能力。

## 状态说明

- 已实现：四个移动入口、共用公开搜索、主题活动列表、单窗口 Agents 需求提交、对接通知、收藏、本人会籍和续费状态、公开资料待审核更新。
- 原型状态：浏览器演示身份、语音录音提示、续费支付提示、在职材料安全元数据提交。原型不会伪装已取得微信资料、已完成语音转写、AI 匹配或支付。
- 正式依赖：公司主体 AppID、微信登录、微信支付、订阅消息、录音授权与 ASR、生产数据库、私有对象存储、文件安全检查、隐私协议与授权留痕。

## 不得突破的安全规则

1. 会籍由微信身份绑定、有效期、群内状态与有效付款/人工复核综合判断；付款证据不能单独激活会籍。
2. 内部 CRM、付款证据、最终会籍、公开名册是独立数据域；会员端仅接收白名单字段。
3. 公开名册必须由会员明确同意并经人工审核；联系方式不公开，默认通过申请对接。
4. 手机号、微信、名片内容、CRM 备注、订单、付款证据、飞书来源、外部 ID、存储路径、会议链接和高敏感材料不得进入搜索、动态、演示数据或普通会员响应。
5. 私密机会只允许 Agent 在私有库和已授权公开资料间受控匹配；申请人看不到私密库或联系人。
6. AI 初筛后必须人工终审；联系人共享须被申请者明确同意并由运营受控履行，接口同意响应仍不返回联系方式。
7. 活动报名不承诺腾讯会议席位；会议链接仅在指定时间窗向会籍有效且已报名会员开放。
8. 公开资料编辑保存为独立更新记录，审核通过前不得改写已发布名册；在职材料只进入内部核验队列且不自动改变会籍或公开资料。

## 390px 视觉检查

- 验收视口：390 × 844；截图见 `docs/member-mobile-390.png`。
- 固定底栏宽度 390px、高度 62px，贴合视口底部；主内容独立滚动，不被底栏遮挡。
- 动态、资料库、Agents、我的四个入口均可切换；白底、`#F5F5F5` 分区、细分割线和系统字体保持微信原生基线，当前状态与主要操作使用克制的浅紫/中紫强调。
- 中紫强调文字及紫底白字主要按钮均达到至少 4.5:1 的对比度；正文黑灰与警告、收藏等语义色保持独立，没有全部紫色化。
- 六个资料分栏可横向滚动；下载关闭时没有按钮，开启时仍只能得到“私有存储待配置”的安全提示。
- 浏览器控制台无错误或警告；运营后台入口及原六模块导航未受影响。
