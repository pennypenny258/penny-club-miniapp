# 仅供一次性、受控迁移任务识别变量类别；本文件不含也不能保存实际值。
DATABASE_URL=
DATABASE_SSL_MODE=verify-full
DATABASE_SCHEMA=venture_private
DATABASE_ROLE_PURPOSE=migrator

# 迁移角色只在短时任务中使用；应能建 schema/table/policy/grant，但不作为长期应用账号。
# 迁移结束后撤销临时访问，并由应用服务使用 venture_club_app 最小权限角色。
